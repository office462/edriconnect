import { createClientFromRequest } from 'npm:@base44/sdk@0.8.31';

Deno.serve(async (req) => {
  try {
    const base44 = createClientFromRequest(req);
    const user = await base44.auth.me();
    if (user?.role !== 'admin') {
      return Response.json({ error: 'Forbidden' }, { status: 403 });
    }

    const { accessToken } = await base44.asServiceRole.connectors.getConnection('googledrive');

    const docTitle = 'מסמך בדיקות בוט — ד"ר ליאת אדרי (EdryConnect)';

    const docContent = `
מסמך בדיקות בוט — ד"ר ליאת אדרי
====================================
תאריך: ${new Date().toLocaleDateString('he-IL')}
גרסה: 1.0

════════════════════════════════════════
הוראות כלליות לבודק
════════════════════════════════════════

מי אתה בבדיקה:
אתה מדמה פונה אמיתי. אתה שולח הודעות בוואטסאפ למספר של ד"ר ליאת אדרי, ובודק שהבוט מגיב נכון בכל שלב.

איך הבדיקה עובדת:
• שלח הודעות בוואטסאפ כאילו אתה לקוח/ה חדש/ה.
• בדוק שהבוט עונה נכון, שולח סרטונים/קבצים/קישורים נכונים.
• סמן בטבלה: ✅ עבר / ❌ נכשל + הערות.

⚠️ חשוב מאוד — ניקוי בין בדיקות:
אחרי כל בדיקת מסלול, חובה לנקות את הנתונים שנוצרו. בלי זה הבדיקה הבאה לא תעבוד נכון כי הבוט יזכור אותך מהפעם הקודמת.

════════════════════════════════════════
הוראות כניסה למערכת הניהול (דשבורד)
════════════════════════════════════════

1. גלוש לכתובת האפליקציה (תקבל אותה בנפרד).
2. הזן את המייל והסיסמה שקיבלת.
3. ייתכן שיישלח קוד אימות לד"ר ליאת — תאם איתה מראש שתעביר לך את הקוד כשתצטרך.
4. לאחר כניסה, תראה את מסך הניהול הראשי.

════════════════════════════════════════
הוראות ניקוי אחרי כל בדיקה (חובה!)
════════════════════════════════════════

אחרי כל בדיקת מסלול, בצע את הפעולות הבאות בדשבורד:

שלב 1 — מחיקת איש קשר:
• לחץ על "אנשי קשר" בתפריט הצד.
• חפש את השם שהזנת בבדיקה.
• לחץ על שלוש הנקודות ← מחק.

שלב 2 — מחיקת פנייה:
• לחץ על "פניות שירות" בתפריט הצד.
• חפש את הפנייה שנוצרה (לפי השם שלך או לפי התאריך האחרון).
• לחץ על שלוש הנקודות ← מחק.

שלב 3 — מחיקת הודעות לוג:
• לחץ על "Data" (או "נתונים") בתפריט העליון.
• בחר בטבלה "WhatsAppMessageLog".
• סנן לפי מספר הטלפון שלך.
• סמן את כל השורות ומחק.

שלב 4 — מחיקת הגדרת שיחה:
• באותו מסך Data, בחר בטבלה "SystemSetting".
• חפש רשומה שה-key שלה מתחיל ב-"phone_conv_" ואחריו מספר הטלפון שלך (בלי 0, עם 972 בהתחלה).
• מחק אותה.

⚠️ אם לא תנקה — הבוט יזכור אותך ויתנהג אחרת בבדיקה הבאה!

════════════════════════════════════════
הוראות לשלבים מיוחדים בבדיקה
════════════════════════════════════════

🔹 שלב תשלום (בכל מסלול שדורש תשלום):
בבדיקה אתה לא משלם באמת. כשמגיעים לשלב התשלום:
1. הבוט ישלח לך קישור לתשלום ו/או ברקוד ביט — בדוק שהקישור נפתח, אבל אל תשלם.
2. היכנס לדשבורד ← פניות שירות ← מצא את הפנייה שלך.
3. לחץ על הפנייה ← שנה סטטוס ל-"שולם" (paid).
4. חזור לוואטסאפ ובדוק שהבוט ממשיך לשלב הבא אוטומטית.

🔹 שלב תשלום בהעברה בנקאית (מסלול משפטי בלבד):
1. הבוט ישלח לך פרטי חשבון בנק — בדוק שהפרטים מופיעים.
2. שלח תמונה כלשהי (סתם צילום מסך) כאילו זו אסמכתא.
3. בדוק שהבוט עונה שקיבל ושהפנייה ממתינה לאישור.
4. היכנס לדשבורד ← שנה סטטוס ל-"שולם" (paid).
5. חזור לוואטסאפ ובדוק שהבוט ממשיך אוטומטית.

🔹 שלב שאלון (מסלול ייעוץ):
בפעם הראשונה — מלא את השאלון באמת דרך הקישור שהבוט שולח.
בבדיקות נוספות — אפשר לדלג: היכנס לדשבורד ← פניות שירות ← שנה סטטוס ל-"שאלון מולא" (questionnaire_completed).

🔹 שלב קביעת תור (Cal.com):
בפעם הראשונה בכל מסלול — קבע תור אמיתי דרך הקישור. בדוק שהבוט מגיב נכון.
בבדיקות נוספות — אפשר לדלג:
• במסלול ייעוץ: שנה סטטוס ל-"תואם תור וואטסאפ" (scheduled_whatsapp) לתור הראשון, ו-"נקבע תור" (scheduled) לתור השני.
• בשאר המסלולים: שנה סטטוס ל-"נקבע תור" (scheduled).

════════════════════════════════════════════════════════════════
בדיקה 1 — מסלול ייעוץ (consultation) — אוטיזם
════════════════════════════════════════════════════════════════

מטרה: לבדוק את מסלול הייעוץ המלא עם בחירת אוטיזם.

--- שלב 1: פתיחת שיחה ---
📱 שלח: "שלום"
✅ בדוק: הבוט מבקש ממך שם מלא, מספר טלפון וכתובת מייל.
📱 שלח: שם מלא + טלפון (05XXXXXXXX) + מייל
✅ בדוק: הבוט מציג את הפרטים שלך ומבקש אישור.
📱 שלח: "כן"
✅ בדוק: הבוט שולח תפריט שירותים (welcome) עם 5 אפשרויות.

--- שלב 2: בחירת ייעוץ ---
📱 שלח: "2" (ייעוץ)
✅ בדוק: הבוט שולח סרטון כללי על התהליך + בקשה לצפות ולכתוב "צפיתי".
✅ בדוק: הסרטון נפתח/מתנגן.

--- שלב 3: צפיתי ---
📱 שלח: "צפיתי"
✅ בדוק: הבוט שולח תפריט בחירת נושא (אוטיזם / מחלות כרוניות).

--- שלב 4: בחירת אוטיזם ---
📱 שלח: "1" (אוטיזם)
✅ בדוק: הבוט שולח סרטון על אוטיזם + בקשה לכתוב "צפיתי".
✅ בדוק: הסרטון נפתח/מתנגן.

--- שלב 5: צפיתי (אוטיזם) ---
📱 שלח: "צפיתי"
✅ בדוק: הבוט מציע חומר נוסף לקריאה באתר.

--- שלב 6: חומר נוסף ---
📱 שלח: "כן"
✅ בדוק: הבוט שולח קישור לאתר + שואל אם להמשיך בתהליך.
✅ בדוק: הקישור נפתח.
📱 שלח: "כן"
✅ בדוק: הבוט שולח הודעת סודיות/פרטיות + שואל אם מסכים.

--- שלב 7: סודיות ---
📱 שלח: "כן"
✅ בדוק: הבוט שולח קישור לשאלון בלבד (בלי תשלום!).
✅ בדוק: קישור השאלון נפתח.

--- שלב 8: שאלון ---
(בפעם הראשונה — מלא את השאלון באמת. בפעמים הבאות — שנה סטטוס בדשבורד ל-"שאלון מולא")
✅ בדוק: אחרי מילוי — הבוט שולח הודעה אוטומטית עם בקשת תשלום + קישור Paybox + ברקוד ביט.
✅ בדוק: קישור התשלום נפתח.

--- שלב 9: תשלום ---
(אל תשלם באמת! היכנס לדשבורד ← פניות שירות ← שנה סטטוס ל-"שולם")
✅ בדוק: הבוט שולח אוטומטית הודעה עם הסבר על 2 פגישות + קישור לתור ווצאפ בלבד.
✅ בדוק: הקישור לתיאום נפתח.
⚠️ חשוב: צריך להיות רק קישור אחד (תור ווצאפ), לא שניים!

--- שלב 10: קביעת תור ווצאפ ---
(בפעם הראשונה — קבע תור אמיתי ב-Cal.com. בפעמים הבאות — שנה סטטוס ל-"תואם תור וואטסאפ")
✅ בדוק: הבוט שולח אישור + קישור לתור ייעוץ מלא (התור השני).
✅ בדוק: הקישור השני נפתח.

--- שלב 11: קביעת תור ייעוץ מלא ---
(בפעם הראשונה — קבע תור אמיתי. בפעמים הבאות — שנה סטטוס ל-"נקבע תור")
✅ בדוק: הבוט שולח אישור שני תורים + הנחיות הגעה + קישור Waze + תמונת מיקום.
✅ בדוק: קישור Waze נפתח.
✅ בדוק: תמונת המיקום מוצגת.

--- שלב 12: סיום ---
📱 שלח: "סיום"
✅ בדוק: הבוט שולח הודעת פרידה.

📋 ניקוי: בצע ניקוי מלא לפי הוראות הניקוי למעלה.
סטטוס: ✅ / ❌
הערות: _______________________________________________

════════════════════════════════════════════════════════════════
בדיקה 2 — מסלול ייעוץ — מחלות כרוניות (פוריות)
════════════════════════════════════════════════════════════════

מטרה: לבדוק את מסלול הייעוץ עם בחירת מחלה כרונית — פוריות.

--- שלבים 1-3: זהים לבדיקה 1 ---
(פתיחת שיחה, פרטים, אישור, welcome, בחירת "2", צפייה בסרטון כללי, "צפיתי")

--- שלב 4: בחירת מחלות כרוניות ---
📱 שלח: "2" (מחלות כרוניות)
✅ בדוק: הבוט שולח רשימת מחלות ממוספרת.

--- שלב 5: בחירת פוריות ---
📱 שלח: "1" (פוריות)
✅ בדוק: הבוט שולח סרטון על פוריות + PDF + בקשה לכתוב "צפיתי וקראתי".
✅ בדוק: הסרטון נפתח.
✅ בדוק: ה-PDF נשלח כקובץ מצורף (לא כקישור טקסט).

--- שלב 6: צפיתי וקראתי ---
📱 שלח: "צפיתי וקראתי"
✅ בדוק: הבוט מציע חומר נוסף לקריאה.

--- שלבים 7-12: זהים לבדיקה 1 ---
(חומר נוסף → סודיות → שאלון → תשלום → תור ווצאפ → תור מלא → הנחיות הגעה → סיום)

📋 ניקוי: בצע ניקוי מלא.
סטטוס: ✅ / ❌
הערות: _______________________________________________

════════════════════════════════════════════════════════════════
בדיקה 3 — מסלול ייעוץ — כל מחלה כרונית (חזרה מהירה)
════════════════════════════════════════════════════════════════

⚠️ חזור על שלבים 1-6 מבדיקה 2 עבור כל מחלה. הבדיקה המהירה בודקת רק שהסרטון וה-PDF הנכונים נשלחים.

לכל מחלה — בדוק:
✅ סרטון מתאים נשלח ונפתח
✅ PDF מתאים נשלח כקובץ (לא כקישור)
✅ הבוט מבקש "צפיתי וקראתי"

רשימת מחלות לבדיקה:
□ הריון (מספר 2)
□ גיל המעבר (מספר 3)
□ סוכרת (מספר 4)
□ דיכאון (מספר 5)
□ מחלות מעי (מספר 6)
□ סרטן (מספר 7)
□ אוטיזם (מספר 8 — בדיקה ספציפית: אין PDF, רק סרטון. הבוט צריך לבקש "צפיתי" ולא "צפיתי וקראתי")

📋 ניקוי: בצע ניקוי מלא אחרי כל מחלה.
הערות: _______________________________________________

════════════════════════════════════════════════════════════════
בדיקה 4 — מסלול חוות דעת משפטית (legal)
════════════════════════════════════════════════════════════════

מטרה: לבדוק את מסלול חוות הדעת המשפטית המלא.

--- שלב 1: פתיחת שיחה + פרטים ---
(זהה לבדיקה 1 — שלב 1)

--- שלב 2: בחירת מסלול משפטי ---
📱 שלח: "3" (חוות דעת משפטית)
✅ בדוק: הבוט שולח הסבר על השירות המשפטי + שואל אם להמשיך להסכם.

--- שלב 3: חומר נוסף ---
📱 שלח: "כן"
✅ בדוק: הבוט מציע חומר נוסף לקריאה באתר.
📱 שלח: "כן"
✅ בדוק: הבוט שולח קישור + שואל אם להמשיך.
📱 שלח: "כן"

--- שלב 4: הסכם ---
✅ בדוק: הבוט שולח הודעה + קובץ PDF של ההסכם (כקובץ מצורף, לא כקישור).
✅ בדוק: ה-PDF נפתח.
📱 שלח: "קראתי"

--- שלב 5: תשלום בהעברה בנקאית ---
✅ בדוק: הבוט שולח פרטי חשבון בנק להעברה.
⚠️ חשוב: אסור שיופיע ברקוד ביט או קישור Paybox! רק פרטי בנק!
📱 שלח: תמונה כלשהי (כאילו אסמכתא של העברה)
✅ בדוק: הבוט עונה שקיבל ושהפנייה ממתינה לאישור.

--- שלב 6: אישור תשלום ---
(היכנס לדשבורד ← פניות שירות ← שנה סטטוס ל-"שולם")
✅ בדוק: הבוט שולח אוטומטית הודעת סודיות + בקשת מסמכים למייל + כתובת מייל.
⚠️ חשוב: אסור שיופיע שאלון! אין שאלון במסלול משפטי!

--- שלב 7: מסמכים ---
📱 שלח: "שלחתי"
✅ בדוק: הבוט מאשר קבלת מסמכים + שולח קישור לתיאום פגישה.
✅ בדוק: קישור התיאום נפתח.

--- שלב 8: קביעת תור ---
(בפעם הראשונה — קבע תור. בפעמים הבאות — שנה סטטוס ל-"נקבע תור")
📱 שלח: "קבעתי"
✅ בדוק: הבוט שולח אישור + הנחיות הגעה + קישור Waze + תמונת מיקום.

--- שלב 9: סיום ---
📱 שלח: "סיום"
✅ בדוק: הבוט שולח הודעת פרידה.

📋 ניקוי: בצע ניקוי מלא.
סטטוס: ✅ / ❌
הערות: _______________________________________________

════════════════════════════════════════════════════════════════
בדיקה 5 — מסלול הרצאות — סדרת הרצאות
════════════════════════════════════════════════════════════════

מטרה: לבדוק את מסלול ההרצאות עם בחירת סדרה.

--- שלב 1: פתיחת שיחה + פרטים ---
(זהה לבדיקה 1 — שלב 1)

--- שלב 2: בחירת הרצאות ---
📱 שלח: "4" (הרצאות)
✅ בדוק: הבוט שולח תפריט עם 3 אפשרויות (סדרה / הרצאה בודדת / סדנת ביופידבק).

--- שלב 3: בחירת סדרה ---
📱 שלח: "1" (סדרה)
✅ בדוק: הבוט שולח קישור לדף הסדרה + בקשה לכתוב "קראתי".
✅ בדוק: הקישור נפתח.

--- שלב 4: קראתי ---
📱 שלח: "קראתי"
✅ בדוק: הבוט שולח קישור לתיאום פגישה.
✅ בדוק: הקישור נפתח.

--- שלב 5: קביעת תור ---
(בפעם הראשונה — קבע תור. בפעמים הבאות — שנה סטטוס)
📱 שלח: "קבעתי"
✅ בדוק: הבוט שולח אישור + הנחיות הגעה + קישור Waze + תמונת מיקום.

--- שלב 6: סיום ---
📱 שלח: "סיום"
✅ בדוק: הבוט שולח הודעת פרידה.

📋 ניקוי: בצע ניקוי מלא.
סטטוס: ✅ / ❌
הערות: _______________________________________________

════════════════════════════════════════════════════════════════
בדיקה 6 — מסלול הרצאות — הרצאה בודדת
════════════════════════════════════════════════════════════════

--- שלבים 1-2: זהים לבדיקה 5 ---

--- שלב 3: בחירת הרצאה בודדת ---
📱 שלח: "2" (הרצאה בודדת)
✅ בדוק: הבוט שולח רשימת הרצאות ממוספרת.

--- שלב 4: בחירת הרצאה ---
📱 שלח: "1" (הרצאה ראשונה ברשימה)
✅ בדוק: הבוט שולח סרטון ו/או PDF של ההרצאה.
✅ בדוק: אם יש סרטון — הוא נפתח.
✅ בדוק: אם יש PDF — הוא נשלח כקובץ מצורף.
✅ בדוק: הבוט מבקש "צפיתי" / "קראתי" / "צפיתי וקראתי" בהתאם לתוכן שנשלח.

--- שלב 5: צפיתי/קראתי ---
📱 שלח: את הביטוי המתאים
✅ בדוק: הבוט שולח קישור לתיאום.

--- שלבים 6-7: זהים לבדיקה 5 (קביעת תור + סיום) ---

📋 ניקוי: בצע ניקוי מלא.
סטטוס: ✅ / ❌
הערות: _______________________________________________

════════════════════════════════════════════════════════════════
בדיקה 7 — מסלול הרצאות — סדנת ביופידבק
════════════════════════════════════════════════════════════════

--- שלבים 1-2: זהים לבדיקה 5 ---

--- שלב 3: בחירת ביופידבק ---
📱 שלח: "3" (ביופידבק)
✅ בדוק: הבוט שולח סרטון + PDF של הסדנה.
✅ בדוק: הסרטון נפתח.
✅ בדוק: ה-PDF נשלח כקובץ מצורף.
✅ בדוק: הבוט מבקש "צפיתי וקראתי".

--- שלב 4: צפיתי וקראתי ---
📱 שלח: "צפיתי וקראתי"
✅ בדוק: הבוט שולח קישור לתיאום.

--- שלבים 5-6: זהים לבדיקה 5 (קביעת תור + סיום) ---

📋 ניקוי: בצע ניקוי מלא.
סטטוס: ✅ / ❌
הערות: _______________________________________________

════════════════════════════════════════════════════════════════
בדיקה 8 — מסלול קליניקה — שוכר ותיק
════════════════════════════════════════════════════════════════

מטרה: לבדוק את מסלול הקליניקה עבור שוכר ותיק.

--- שלב 1: פתיחת שיחה + פרטים ---
(זהה לבדיקה 1 — שלב 1)

--- שלב 2: בחירת קליניקה ---
📱 שלח: "5" (קליניקה)
✅ בדוק: הבוט שואל אם שוכר ותיק או מתעניין חדש.

--- שלב 3: שוכר ותיק ---
📱 שלח: "1" (ותיק)
✅ בדוק: הבוט שואל אם צריך קוד סודי.

--- שלב 4א: כן, צריך קוד ---
📱 שלח: "כן"
✅ בדוק: הבוט שולח את הקוד הסודי.
✅ בדוק: הבוט שואל "האם יש משהו נוסף?"

--- שלב 5: אין משהו נוסף ---
📱 שלח: "לא"
✅ בדוק: הבוט שולח הודעת פרידה.

📋 ניקוי: בצע ניקוי מלא.

--- בדיקה 8ב: שוכר ותיק — יש משהו נוסף ---
חזור על שלבים 1-4, ואז:
📱 שלח: "כן" (יש משהו נוסף)
✅ בדוק: הבוט מפנה לשלום (מנהל הקליניקה) עם מספר טלפון.
✅ בדוק: הבוט שולח הודעת פרידה.

📋 ניקוי: בצע ניקוי מלא.
סטטוס: ✅ / ❌
הערות: _______________________________________________

════════════════════════════════════════════════════════════════
בדיקה 9 — מסלול קליניקה — מתעניין חדש
════════════════════════════════════════════════════════════════

מטרה: לבדוק מתעניין חדש שבוחר חדר.

--- שלב 1: פתיחת שיחה + פרטים ---
(זהה לבדיקה 1 — שלב 1)

--- שלב 2: בחירת קליניקה → חדש ---
📱 שלח: "5" (קליניקה)
📱 שלח: "2" (מתעניין חדש)
✅ בדוק: הבוט שולח תפריט בחירת חדר (1=חדר שיחה, 2=חדר טיפול, 3=חדר רופא).

--- שלב 3: בחירת חדר ---
📱 שלח: "1" (חדר שיחה)
✅ בדוק: הבוט שולח תמונה של חדר שיחה (כקובץ מצורף) + סרטון.
✅ בדוק: התמונה מוצגת.
✅ בדוק: הסרטון נפתח.
✅ בדוק: הבוט מבקש "צפיתי".

--- שלב 4: צפיתי ---
📱 שלח: "צפיתי"
✅ בדוק: הבוט שולח מידע על הקליניקה + מחירון.
✅ בדוק: הבוט מבקש "קראתי".

--- שלב 5: קראתי ---
📱 שלח: "קראתי"
✅ בדוק: הבוט שולח קישור לתיאום הגעה.
✅ בדוק: הקישור נפתח.

--- שלב 6: קביעת תור ---
📱 שלח: "קבעתי"
✅ בדוק: הבוט שולח אישור + הנחיות הגעה + Waze + תמונת מיקום.

--- שלב 7: תשלום ---
(הבוט ישלח הודעה שמזמינה לשלם כשהפונה מוכן)
📱 שלח: "מעוניינת לשלם"
✅ בדוק: הבוט שולח קישור תשלום + ברקוד ביט.
(היכנס לדשבורד ← שנה סטטוס ל-"שולם")
✅ בדוק: הבוט שולח אישור תשלום + PDF התנהלות + הודעת סיום.

📋 ניקוי: בצע ניקוי מלא.

⚠️ חזור על בדיקה 9 גם עם חדר טיפול (מספר 2) וחדר רופא (מספר 3).
בדוק שכל חדר מציג תמונה וסרטון שונים!

סטטוס: ✅ / ❌
הערות: _______________________________________________

════════════════════════════════════════════════════════════════
בדיקה 10 — מסלול פוסט הרצאה (QR)
════════════════════════════════════════════════════════════════

מטרה: לבדוק את מסלול הפוסט הרצאה — כאילו סרקת QR בסוף הרצאה.

--- שלב 1: הודעת QR ---
📱 שלח: "הי ד"ר אדרי, אשמח לקבל את הסיכום של ההרצאה - אנטומיה של אושר"
✅ בדוק: הבוט שולח PDF סיכום (כקובץ מצורף) + קישור לבלוג.
✅ בדוק: ה-PDF נפתח.
✅ בדוק: קישור הבלוג נפתח.
⚠️ חשוב: הבוט לא מבקש פרטים לפני שליחת ה-PDF! ה-PDF נשלח מיד!

--- שלב 2: תגובה חיובית ---
📱 שלח: "תודה" (או "קיבלתי")
✅ בדוק: הבוט שולח הזמנה לרשימת דיוור + מבקש פרטים (שם + טלפון + מייל).
⚠️ חשוב: "תודה" כאן זו לא הודעת פרידה! הבוט צריך להמשיך לשלב הבא!

--- שלב 3: שליחת פרטים ---
📱 שלח: שם + טלפון + מייל
✅ בדוק: הבוט מאשר שהפרטים נשמרו.
✅ בדוק: הבוט שולח המלצה על סדרת ההרצאות + תמונה של הסדרה + הודעת סיום.

📋 ניקוי: בצע ניקוי מלא.
סטטוס: ✅ / ❌
הערות: _______________________________________________

════════════════════════════════════════════════════════════════
בדיקה 11 — בדיקות התנהגות כללית
════════════════════════════════════════════════════════════════

מטרה: לבדוק תגובות הבוט למצבים מיוחדים.

--- 11א: שאלה לא רלוונטית ---
(פתח שיחה רגילה עם פרטים + welcome)
📱 שלח: "מה מזג האוויר היום?"
✅ בדוק: הבוט עונה שהוא עוסק רק בנושאי ד"ר אדרי.
📋 ניקוי.

--- 11ב: בקשת נציג ---
(פתח שיחה + היכנס לאמצע מסלול)
📱 שלח: "אני רוצה לדבר עם ד"ר ליאת"
✅ בדוק: הבוט מנסה להחזיר למסלול (ניסיון ראשון).
📱 שלח: "אני רוצה נציג אנושי!"
✅ בדוק: הבוט מנסה שוב (ניסיון שני).
📱 שלח: "אני מתעקש!"
✅ בדוק: הבוט מציע לבחור — להמשיך בתהליך או להמתין לד"ר ליאת.
📋 ניקוי.

--- 11ג: הודעת פרידה ---
(פתח שיחה + היכנס למסלול)
📱 שלח: "תודה רבה, יום טוב"
✅ בדוק: הבוט עונה בפרידה קצרה ולא שולח שום דבר נוסף.
📋 ניקוי.

--- 11ד: טריגר "סיום" ---
(פתח שיחה + היכנס למסלול)
📱 שלח: "סיום"
✅ בדוק: הבוט שולח הודעת פרידה ונעצר.
📋 ניקוי.

--- 11ה: חזרה לתפריט ---
(פתח שיחה + סיים מסלול אחד)
📱 שלח: "אפשר לשמוע על שירות נוסף?"
✅ בדוק: הבוט שולח שוב את תפריט השירותים (welcome).
📋 ניקוי.

סטטוס: ✅ / ❌
הערות: _______________________________________________

════════════════════════════════════════════════════════════════
טבלת סיכום
════════════════════════════════════════════════════════════════

בדיקה | מסלול | תוצאה | הערות
1 | ייעוץ — אוטיזם | ✅/❌ |
2 | ייעוץ — פוריות | ✅/❌ |
3 | ייעוץ — כל מחלה (מהירה) | ✅/❌ |
4 | משפטי | ✅/❌ |
5 | הרצאות — סדרה | ✅/❌ |
6 | הרצאות — בודדת | ✅/❌ |
7 | הרצאות — ביופידבק | ✅/❌ |
8 | קליניקה — ותיק | ✅/❌ |
9 | קליניקה — חדש (×3 חדרים) | ✅/❌ |
10 | פוסט הרצאה (QR) | ✅/❌ |
11 | התנהגות כללית | ✅/❌ |

════════════════════════════════════════════════════════════════
סוף מסמך בדיקות
════════════════════════════════════════════════════════════════
`;

    // Create Google Doc
    const createDocResp = await fetch('https://www.googleapis.com/drive/v3/files', {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${accessToken}`,
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({
        name: docTitle,
        mimeType: 'application/vnd.google-apps.document',
      }),
    });

    if (!createDocResp.ok) {
      const err = await createDocResp.text();
      return Response.json({ error: `Failed to create doc: ${err}` }, { status: 500 });
    }

    const docData = await createDocResp.json();
    const fileId = docData.id;

    // Upload content as plain text to the doc using Google Docs API
    // First, update the doc content using Docs API batchUpdate
    const docsResp = await fetch(`https://docs.googleapis.com/v1/documents/${fileId}:batchUpdate`, {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${accessToken}`,
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({
        requests: [
          {
            insertText: {
              location: { index: 1 },
              text: docContent,
            },
          },
        ],
      }),
    });

    // If Docs API fails (scope limitation), try uploading as plain text
    if (!docsResp.ok) {
      // Delete the empty doc
      await fetch(`https://www.googleapis.com/drive/v3/files/${fileId}`, {
        method: 'DELETE',
        headers: { 'Authorization': `Bearer ${accessToken}` },
      });

      // Upload as Google Doc by converting from text/plain
      const uploadResp = await fetch(
        'https://www.googleapis.com/upload/drive/v3/files?uploadType=multipart&convert=true',
        {
          method: 'POST',
          headers: {
            'Authorization': `Bearer ${accessToken}`,
          },
          body: createMultipartBody(docTitle, docContent),
        }
      );

      if (!uploadResp.ok) {
        // Last resort: upload as plain text file
        const boundary = 'boundary_test_doc';
        const metadata = JSON.stringify({
          name: docTitle + '.txt',
          mimeType: 'text/plain',
        });
        const multipartBody = `--${boundary}\r\nContent-Type: application/json; charset=UTF-8\r\n\r\n${metadata}\r\n--${boundary}\r\nContent-Type: text/plain; charset=UTF-8\r\n\r\n${docContent}\r\n--${boundary}--`;

        const txtResp = await fetch(
          'https://www.googleapis.com/upload/drive/v3/files?uploadType=multipart',
          {
            method: 'POST',
            headers: {
              'Authorization': `Bearer ${accessToken}`,
              'Content-Type': `multipart/related; boundary=${boundary}`,
            },
            body: multipartBody,
          }
        );

        if (!txtResp.ok) {
          const err = await txtResp.text();
          return Response.json({ error: `Failed to upload: ${err}` }, { status: 500 });
        }

        const txtData = await txtResp.json();
        return Response.json({
          ok: true,
          fileId: txtData.id,
          fileName: txtData.name,
          url: `https://drive.google.com/file/d/${txtData.id}/view`,
          note: 'Saved as text file (Google Docs API not available with current scope)',
        });
      }

      const uploadData = await uploadResp.json();
      return Response.json({
        ok: true,
        fileId: uploadData.id,
        fileName: uploadData.name,
        url: `https://docs.google.com/document/d/${uploadData.id}/edit`,
      });
    }

    return Response.json({
      ok: true,
      fileId,
      fileName: docData.name,
      url: `https://docs.google.com/document/d/${fileId}/edit`,
    });
  } catch (error) {
    console.error('createTestDocument error:', error);
    return Response.json({ error: error.message }, { status: 500 });
  }
});

function createMultipartBody(title, content) {
  const boundary = 'test_doc_boundary';
  const metadata = JSON.stringify({
    name: title,
    mimeType: 'application/vnd.google-apps.document',
  });
  
  const body = `--${boundary}\r\nContent-Type: application/json; charset=UTF-8\r\n\r\n${metadata}\r\n--${boundary}\r\nContent-Type: text/plain; charset=UTF-8\r\n\r\n${content}\r\n--${boundary}--`;

  return new Blob([body], { type: `multipart/related; boundary=${boundary}` });
}